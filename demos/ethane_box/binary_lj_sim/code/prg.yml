statements:
  statements:
  - save_compound:
      compound: '{{ compound }}'
      forcefield: '{{ forcefield }}'
      system_name: '{{ system_name }}'
  - binary_lj_sim:
      r_cut:
      system_name: '{{ system_name }}'
      T:
      tau:
      steps:
